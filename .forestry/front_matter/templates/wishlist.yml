---
label: List
hide_body: true
display_field: title
fields:
- name: title
  type: text
  config:
    required: true
  label: Title
- name: item
  type: field_group_list
  fields:
  - name: text
    type: text
    config:
      required: true
    label: Text
    hidden: false
  - name: url
    type: text
    config:
      required: false
    label: Url
  - name: image
    type: file
    config:
      maxSize: 64
    label: Image
  config:
    min: 
    max: 
    labelField: 
  label: Item
  description: Items for on the wishlist
pages:
- static_pages/wishlist.md
