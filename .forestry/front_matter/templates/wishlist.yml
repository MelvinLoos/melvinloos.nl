---
label: Wishlist
hide_body: true
fields:
- name: item
  type: field_group_list
  fields:
  - name: text
    type: text
    config:
      required: true
    label: Text
    hidden: false
  - name: url
    type: text
    config:
      required: false
    label: Url
  - name: image
    type: file
    config:
      maxSize: 64
    label: Image
  config:
    min: 
    max: 
    labelField: 
  label: Item
  description: Item for on the wishlist
pages:
- static_pages/wishlist.md
