---
label: Blog post
hide_body: false
fields:
- name: title
  type: text
  config:
    required: true
  label: Title
- name: image
  type: file
  config:
    maxSize: 64
  label: Image
  description: Main image to accompany the article, used as thumbnail on the overview
    and on the article page itself
- name: url
  type: text
  config:
    required: true
  label: Url
